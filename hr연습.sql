desc employees;
select *
  from employees;
  
  select last_name
  from employees;
  
  select last_name as "��"
  from employees;
  
  select last_name || ' ' || first_name as "�̸�"
  from employees;